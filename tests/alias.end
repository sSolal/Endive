Define id, #x -> #x ~ True // id defined

Define b, a
Define c, b

Verify a -> c ~ a in context
Verify id ~ #x in context

Verify #A => B ~ error // B is not buildable

// Test redefinition warning
Define foo, x ~ foo defined
Define foo, y ~ foo defined (redefinition)